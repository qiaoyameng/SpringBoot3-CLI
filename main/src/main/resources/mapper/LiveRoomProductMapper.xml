<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rosy.main.mapper.LiveRoomProductMapper">

    <select id="selectByLiveRoomId" resultType="com.rosy.main.domain.entity.LiveRoomProduct">
        SELECT * FROM live_room_product
        WHERE live_room_id = #{liveRoomId} AND is_deleted = 0
        ORDER BY sort_order ASC
    </select>

    <select id="selectExplainingProduct" resultType="com.rosy.main.domain.entity.LiveRoomProduct">
        SELECT * FROM live_room_product
        WHERE live_room_id = #{liveRoomId} AND is_explaining = 1 AND is_deleted = 0
        LIMIT 1
    </select>

</mapper>
